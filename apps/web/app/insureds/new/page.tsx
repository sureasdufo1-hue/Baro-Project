"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function NewInsured(){const router=useRouter();const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${api}/api/insureds`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),birth_date:f.get("birthDate")||null,gender:f.get("gender"),relationship_type:f.get("relationship")})});if(r.ok)router.push("/contracts/new");else setMsg("피보험자 정보를 확인하세요.")}return <main><section className="card"><h1>피보험자 추가</h1><form onSubmit={submit}><label>이름<input name="name" required/></label><label>생년월일<input name="birthDate" type="date"/></label><label>성별<select name="gender"><option value="UNKNOWN">선택 안 함</option><option value="MALE">남성</option><option value="FEMALE">여성</option></select></label><label>관계<select name="relationship"><option value="SELF">본인</option><option value="SPOUSE">배우자</option><option value="CHILD">자녀</option><option value="PARENT">부모</option><option value="OTHER">기타</option></select></label>{msg&&<p role="alert">{msg}</p>}<button className="button">저장</button></form></section></main>}
