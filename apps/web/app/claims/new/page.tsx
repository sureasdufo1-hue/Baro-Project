"use client";
import {FormEvent,useEffect,useState} from "react";
import {useRouter} from "next/navigation";
const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
type Contract={contract_id:string;insured_id:string;policy_number:string|null;contract_status:string;coverage_start_date:string|null;coverage_end_date:string|null};
export default function NewClaimPage(){const router=useRouter();const [contracts,setContracts]=useState<Contract[]>([]),[type,setType]=useState("DISEASE"),[message,setMessage]=useState("");useEffect(()=>{fetch(`${api}/api/contracts`,{credentials:"include"}).then(r=>r.json()).then(setContracts)},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");const f=new FormData(e.currentTarget);const accident={accident_date:type==="INJURY"?f.get("accidentDate")||null:null,diagnosis_date:type==="DISEASE"?f.get("diagnosisDate")||null:null,onset_date:type==="DISEASE"?f.get("onsetDate")||null:null,location:type==="INJURY"?f.get("location")||null:null,description:f.get("description")||null};const created=await fetch(`${api}/api/claims`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({contract_id:f.get("contract"),claim_type:type,title:f.get("title")||null,accident})});if(!created.ok){setMessage("계약과 사고·질병 정보를 확인하세요.");return}const claim=await created.json();const moved=await fetch(`${api}/api/claims/${claim.claim_id}/transitions`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"SUBMIT_ACCIDENT_INFORMATION"})});if(moved.ok)router.push(`/claims/${claim.claim_id}?created=1`);else setMessage("Case는 저장되었지만 사고정보 완료 처리에 실패했습니다.")}
return <main><section className="card"><div className="stepper"><strong>① 사고정보</strong><span>② 문서등록</span><span>③ 정보확인</span><span>④ 분석</span><span>⑤ 결과</span></div><h1>새로운 보험금 분석</h1><form onSubmit={submit}><fieldset><legend>발생유형</legend><div className="choice-row">{[["DISEASE","질병"],["INJURY","상해"],["OTHER","기타"]].map(([v,l])=><label className="choice" key={v}><input type="radio" name="type" value={v} checked={type===v} onChange={()=>setType(v)}/>{l}</label>)}</div></fieldset><label>분석할 보험계약<select name="contract" required><option value="">선택</option>{contracts.map(x=><option key={x.contract_id} value={x.contract_id}>{x.policy_number??"증권번호 미입력"} · {x.coverage_start_date??"기간 미입력"} · {x.contract_status}</option>)}</select></label><label>Case 제목<input name="title" placeholder="예: 7월 교통사고 골절"/></label>{type==="DISEASE"&&<><label>진단일<input name="diagnosisDate" type="date"/></label><label>증상/발병일<input name="onsetDate" type="date"/></label></>}{type==="INJURY"&&<><label>사고일<input name="accidentDate" type="date" required/></label><label>사고장소<input name="location"/></label></>}<label>{type==="INJURY"?"사고내용":"내용"}<textarea name="description" required={type==="OTHER"}/></label><p className="notice">입력 정보는 사용자 진술이며 문서로 확인된 사실이 아닙니다.</p>{message&&<p role="alert">{message}</p>}<button className="button">Case 생성하고 다음 단계로</button></form></section></main>}
